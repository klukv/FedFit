import type { WorkoutLevel } from "../types/entities";

/** Русские подписи уровней для UI */
export const WORKOUT_LEVEL_LABELS: Record<WorkoutLevel, string> = {
  beginner: "Начинающий",
  intermediate: "Средний",
  advanced: "Продвинутый",
};
